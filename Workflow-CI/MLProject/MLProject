name: workflow-ci-model

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "train_preprocessing.csv"}
      model_output: {type: str, default: "model_output"}
    command: "python modelling.py --data_path {data_path} --model_output {model_output}"
